{
  "name": "collect-squares-game",
  "version": "1.0.0",
  "description": "game to collect treats on the board",
  "keywords": [
    "testing",
    "game"
  ],
  "main": "src/index.js",
  "dependencies": {
    "react": "16.4.2",
    "react-dom": "16.4.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && surge -p build/ -d https://trickortreat.surge.sh"
  },
  "devDependencies": {
    "@testing-library/react": "^9.3.0"
  }
}
